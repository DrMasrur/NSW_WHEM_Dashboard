# NSW_WHEM_Dashboard

A lightweight visualization dashboard for NSW WHEM facility occupancy.

## Run locally

Because this is a static dashboard, you can serve it with any simple web server:

```bash
python3 -m http.server 8000
```

Then open `http://localhost:8000`.
